<UserControl x:Class="IntelliStretch.UI.UIPrelimSettings"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:UserControls="clr-namespace:IntelliStretch.UserControls"
    xmlns:value_converters="clr-namespace:IntelliStretch.ValueConverters"
    x:Name="uiPrelimSettings" Loaded="uiPrelimSettings_Loaded">

    <UserControl.Resources>
        <value_converters:BoolScaleConverter x:Key="boolScaleConverter" />
        
        <!-- Colors -->
        <SolidColorBrush x:Key="PrimaryBlueColor" Color="#348EF6"/>
        <SolidColorBrush x:Key="SecondaryBlueColor" Color="#EFF2FF"/>

        <SolidColorBrush x:Key="PrimaryTextColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SecondaryTextColor" Color="#BBC1D1"/>
        <SolidColorBrush x:Key="TertiaryTextColor" Color="Gray"/>

        <SolidColorBrush x:Key="PrimaryGrayColor" Color="#BBC1D1"/>
        <SolidColorBrush x:Key="SecondaryGrayColor" Color="Gray"/>

        <!-- Start Styles-->

        <!-- Start: TabItem Styles -->
        <Style x:Key="TabItemStyle" TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Margin" Value="0 0 5 0"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid SnapsToDevicePixels="True">
                            <Border CornerRadius="5"
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Margin="{TemplateBinding Margin}"/>
                            <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          SnapsToDevicePixels="True"
                                          RecognizesAccessKey="True"
                                          Margin="{TemplateBinding Padding}"
                                          ContentSource="Header"
                                          Focusable="False"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background"
                                    Value="{DynamicResource SecondaryBlueColor}"/>
                                <Setter Property="Foreground"
                                    Value="{DynamicResource PrimaryGrayColor}"/>

                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background"
                                    Value="{DynamicResource PrimaryBlueColor}"/>
                                <Setter Property="Foreground"
                                    Value="{DynamicResource PrimaryTextColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- End: TabItem Styles -->

        <!-- Start: TabControl Styles -->
        <Style x:Key="TabControlStyle" TargetType="TabControl">
            <Setter Property="Background" Value="{DynamicResource SecondaryBlueColor}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid SnapsToDevicePixels="True">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="50"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TabPanel Grid.Row="0"
                                  Background="Transparent"
                                  IsItemsHost="True"/>
                            <Border Grid.Row="1" 
                                CornerRadius="5"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Background="{TemplateBinding Background}">
                                <ContentPresenter ContentSource="SelectedContent"
                                              SnapsToDevicePixels="True"
                                              Margin="{TemplateBinding Padding}"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- End: TabControl Styles-->

        <!-- Start: Button Style -->
        <Style TargetType="Button" x:Key="GeneralButtonStyle">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="{DynamicResource PrimaryBlueColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextColor}"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid SnapsToDevicePixels="True">
                            <Border CornerRadius="5"
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Margin="{TemplateBinding Margin}"/>
                            <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          SnapsToDevicePixels="True"
                                          RecognizesAccessKey="True"
                                          Margin="{TemplateBinding Margin}"
                                          ContentSource="Content"
                                          Focusable="False"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background"
                                    Value="{DynamicResource PrimaryGrayColor}"/>
                                <Setter Property="Foreground"
                                    Value="{DynamicResource SecondaryBlueColor}"/>

                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>


            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5" />
                </Style>
            </Style.Resources>
        </Style>
        <!-- End: Button Style -->
    </UserControl.Resources>


    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Slider Grid.Column="0" x:Name="sliderValuePicker" Margin="20" Orientation="Vertical" SmallChange="1" IsSnapToTickEnabled="True" ValueChanged="sliderValuePicker_ValueChanged" Grid.RowSpan="1"/>

        <Grid RenderTransformOrigin="0.5,0.5" Grid.Column="2" HorizontalAlignment="Center" Panel.ZIndex="-1">
            <Image x:Name="imgGrid" Source="{Binding GuideLinesImage, ElementName=uiPrelimSettings}" HorizontalAlignment="Right" Margin="50"/>
            <Image x:Name="imgNeutral" Source="{Binding NeutralImage, ElementName=uiPrelimSettings}" HorizontalAlignment="Right" Margin="50"/>
            <Image x:Name="imgFlexion" Source="{Binding FlexionImage, ElementName=uiPrelimSettings}" HorizontalAlignment="Right" Visibility="Collapsed" Margin="50"/>
            <Image x:Name="imgExtension" Source="{Binding ExtensionImage, ElementName=uiPrelimSettings}" HorizontalAlignment="Right" Visibility="Collapsed" Margin="50"/>

            <Border Background="#E6000000" BorderThickness="5" CornerRadius="5" BorderBrush="{DynamicResource SecondaryGrayColor}" Panel.ZIndex="-1"/>
        </Grid>

        <StackPanel Orientation="Vertical" Grid.Column="1">

            <TabControl x:Name="tabPreliminary" Width="235" Style="{DynamicResource TabControlStyle}" Grid.Row="0">
                <TabItem x:Name="tabWrist" Header="Wrist" Style="{DynamicResource TabItemStyle}">
                    <StackPanel Orientation="Vertical">

                        <TextBlock Height="24" Text="Neutral Position" TextWrapping="Wrap" Foreground="{DynamicResource TertiaryTextColor}" FontSize="16" Margin="10,5,5,0"/>
                        <Button 
                                x:Name="setWristNeutral" 
                                Content="Set zero" 
                                GotFocus="setting_GotFocus" 
                                Style="{DynamicResource GeneralButtonStyle}"
                                Click="setNeutral_Click"
                                Height="28" 
                                FontSize="12"
                                Margin="5,0,5,5"/>
                        <UserControls:TextButton 
                                x:Name="setWristFlexion"
                                GotFocus="setting_GotFocus" 
                                Caption="{Binding Path=FlexionName, ElementName=uiPrelimSettings, StringFormat={}Max {0}}"
                                ButtonText="Measure" 
                                ButtonClick="Measure_ButtonClick"/>
                        <UserControls:TextButton
                                x:Name="setWristExtension"
                                GotFocus="setting_GotFocus"
                                Caption="{Binding Path=ExtensionName, ElementName=uiPrelimSettings, StringFormat={}Max {0}}"
                                ButtonText="Measure"
                                ButtonClick="Measure_ButtonClick"/>
                        <Border x:Name="wristExRangeBdr" BorderThickness="2" CornerRadius="5" Grid.Row="4" GotFocus="wristExRangeBdr_GotFocus" LostFocus="wristExRangeBdr_LostFocus">
                            <StackPanel Orientation="Horizontal">
                                <StackPanel Margin="10,5" Orientation="Vertical">
                                    <TextBlock  Height="24" Text="Extra Range" TextWrapping="Wrap" Foreground="{DynamicResource TertiaryTextColor}" FontSize="16"/>
                                    <StackPanel Orientation= "Horizontal">
                                        <TextBox
                                                x:Name="wristRange"
                                                Height="28"
                                                Width="100"
                                                TextWrapping="Wrap"
                                                Padding="10,0"
                                                FontSize="16"
                                                IsReadOnly="True"
                                                VerticalContentAlignment="Center">
                                            <TextBox.Resources>
                                                <Style TargetType="{x:Type Border}">
                                                    <Setter Property="CornerRadius" Value="5"/>
                                                </Style>
                                                <Style TargetType="{x:Type TextBox}">
                                                    <Setter Property="BorderThickness" Value="0"/>
                                                </Style>
                                            </TextBox.Resources>
                                        </TextBox>
                                    </StackPanel>
                                </StackPanel>
                                <UserControls:HorizontalScroller x:Name="wristRangeScroller" Height="28" VerticalAlignment="Bottom" Margin="5" ButtonMinClick="wristRangeScroller_ButtonMinClick" ButtonMaxClick="wristRangeScroller_ButtonMaxClick"/>
                            </StackPanel>

                        </Border>
                    </StackPanel>
                </TabItem>

                <TabItem x:Name="tabMCP" Header="MCP" Style="{DynamicResource TabItemStyle}">
                    <StackPanel Orientation="Vertical">
                        <TextBlock 
                                Height="24" 
                                Text="Neutral Position" 
                                TextWrapping="Wrap" 
                                Foreground="{DynamicResource TertiaryTextColor}"
                                FontSize="16"
                                Margin="10,5,5,0"/>
                        <Button
                                x:Name="setMCPNeutral"
                                Content="Set zero"
                                GotFocus="setting_GotFocus"
                                Style="{DynamicResource GeneralButtonStyle}"
                                Click="setNeutral_Click" 
                                Height="28"
                                FontSize="12"
                                Margin="5,0,5,5"/>
                        <UserControls:TextButton
                                x:Name="setMCPFlexion"
                                GotFocus="setting_GotFocus"
                                Caption="Max Flexion"
                                ButtonText="Measure"
                                ButtonClick="Measure_ButtonClick"/>
                        <UserControls:TextButton
                                x:Name="setMCPExtension"
                                Grid.Row="3" 
                                GotFocus="setting_GotFocus"
                                Caption="Max Extension" 
                                ButtonText="Measure"
                                ButtonClick="Measure_ButtonClick"/>
                        <Border x:Name="mcpExRangeBdr" BorderThickness="2" CornerRadius="5" Grid.Row="4" GotFocus="mcpExRangeBdr_GotFocus" LostFocus="mcpExRangeBdr_LostFocus">
                            <StackPanel Orientation="Horizontal">
                                <StackPanel Margin="10,5">
                                    <TextBlock  Height="24" Text="Extra Range" TextWrapping="Wrap" Foreground="{DynamicResource TertiaryTextColor}" FontSize="16"/>
                                    <StackPanel Orientation= "Horizontal">
                                        <TextBox
                                                 x:Name="mcpRange"
                                                 Height="28"
                                                 Width="100"
                                                 TextWrapping="Wrap"
                                                 Padding="10,0"
                                                 FontSize="16"
                                                 IsReadOnly="True"
                                                 VerticalContentAlignment="Center">
                                            <TextBox.Resources>
                                                <Style TargetType="{x:Type Border}">
                                                    <Setter Property="CornerRadius" Value="5"/>
                                                </Style>
                                                <Style TargetType="{x:Type TextBox}">
                                                    <Setter Property="BorderThickness" Value="0"/>
                                                </Style>
                                            </TextBox.Resources>
                                        </TextBox>
                                    </StackPanel>
                                </StackPanel>
                                <UserControls:HorizontalScroller x:Name="mcpRangeScroller" Height="28" VerticalAlignment="Bottom" Margin="5" ButtonMinClick="mcpRangeScroller_ButtonMinClick" ButtonMaxClick="mcpRangeScroller_ButtonMaxClick" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </TabItem>
            </TabControl>

            <Button x:Name="btnApplyGeneralSettings" Content="Set measures" Style="{DynamicResource GeneralButtonStyle}" Click="btnApplyGeneralSettings_Click" Height="50"/>

            <Border Background="{DynamicResource PrimaryBlueColor}" Grid.Row="1" Height="50" Margin="0,10,0,0" CornerRadius="5,5,0,0">
                <Grid>
                    <TextBlock Text="Control Measures" Margin="10,0" VerticalAlignment="Center" FontWeight="Bold" FontSize="20" Foreground="{DynamicResource PrimaryTextColor}"/>
                </Grid>
            </Border>

            <Border Background="{DynamicResource SecondaryBlueColor}" Grid.Row="2" CornerRadius="0,0,5,5">
                <Grid Margin="10,20">
                    <TextBlock x:Name="txtDataInfo"
                            FontSize="20" 
                            TextWrapping="Wrap" 
                            TextAlignment="Justify"
                            FontWeight="SemiBold"
                            Foreground="{DynamicResource TertiaryTextColor}"/>
                </Grid>
            </Border>

            <UserControls:EmbeddedDialog DialogTitle="Caution" DialogText="Please make sure your stop switch is enabled !" Padding="0,20" Grid.Column="1" DialogWidth="235" VerticalAlignment="Bottom" HorizontalAlignment="Stretch"/>
        </StackPanel>
    </Grid>
</UserControl>
