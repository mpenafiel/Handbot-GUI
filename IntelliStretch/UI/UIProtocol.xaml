<UserControl x:Class="IntelliStretch.UI.UIProtocol"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:UserControls="clr-namespace:IntelliStretch.UserControls"
    xmlns:value_converters="clr-namespace:IntelliStretch.ValueConverters"
    x:Name="uiProtocol">
    <UserControl.Resources>
        <value_converters:BoolReverseConverter x:Key="boolReverseConverter" />
        <Storyboard x:Key="sbSlideIn">
            <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="bdrProtocol" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)">
                <SplineDoubleKeyFrame KeyTime="00:00:00" Value="700"/>
                <SplineDoubleKeyFrame KeyTime="00:00:01" Value="0" KeySpline="0,0.7,0.2,1"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>
        <Storyboard x:Key="sbSlideOut" Completed="aniSlideOut_Completed">
            <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="bdrProtocol" Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)">
                <SplineDoubleKeyFrame KeyTime="00:00:00" Value="0" />
                <SplineDoubleKeyFrame KeyTime="00:00:01" Value="700" KeySpline="0,0.7,0.2,1"/>
            </DoubleAnimationUsingKeyFrames>
        </Storyboard>

        <!-- Colors -->
        <SolidColorBrush x:Key="PrimaryBlueColor" Color="#348EF6"/>
        <SolidColorBrush x:Key="SecondaryBlueColor" Color="#EFF2FF"/>

        <SolidColorBrush x:Key="PrimaryTextColor" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SecondaryTextColor" Color="#BBC1D1"/>
        <SolidColorBrush x:Key="TertiaryTextColor" Color="Gray"/>

        <SolidColorBrush x:Key="PrimaryGrayColor" Color="#BBC1D1"/>
        <SolidColorBrush x:Key="SecondaryGrayColor" Color="Gray"/>


        <!-- Start: TabItem Styles -->
        <Style x:Key="TabItemStyle" TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryTextColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Margin" Value="0 0 5 0"/>
            <Setter Property="Width" Value="200"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid SnapsToDevicePixels="True">
                            <Border CornerRadius="5"
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Margin="{TemplateBinding Margin}"/>
                            <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          SnapsToDevicePixels="True"
                                          RecognizesAccessKey="True"
                                          Margin="{TemplateBinding Padding}"
                                          ContentSource="Header"
                                          Focusable="False"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background"
                                    Value="{DynamicResource SecondaryBlueColor}"/>
                                <Setter Property="Foreground"
                                    Value="{DynamicResource PrimaryGrayColor}"/>

                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background"
                                    Value="{DynamicResource PrimaryBlueColor}"/>
                                <Setter Property="Foreground"
                                    Value="{DynamicResource PrimaryTextColor}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- End: TabItem Styles -->

        <!-- Start: TabControl Styles -->
        <Style x:Key="TabControlStyle" TargetType="TabControl">
            <Setter Property="Background" Value="{DynamicResource SecondaryBlueColor}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabControl">
                        <Grid SnapsToDevicePixels="True">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="50"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TabPanel Grid.Row="0"
                                  Background="Transparent"
                                  IsItemsHost="True"/>
                            <Border Grid.Row="1" 
                                CornerRadius="5"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Background="{TemplateBinding Background}">
                                <ContentPresenter ContentSource="SelectedContent"
                                              SnapsToDevicePixels="True"
                                              Margin="{TemplateBinding Padding}"/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- End: TabControl Styles -->

        <!-- Start: Button Style -->
        <Style TargetType="Button" x:Key="GeneralButtonStyle">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="{DynamicResource PrimaryBlueColor}"/>
            <Setter Property="Foreground" Value="{DynamicResource PrimaryTextColor}"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid SnapsToDevicePixels="True">
                            <Border CornerRadius="5"
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                Margin="{TemplateBinding Margin}"/>
                            <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"
                                          SnapsToDevicePixels="True"
                                          RecognizesAccessKey="True"
                                          Margin="{TemplateBinding Margin}"
                                          ContentSource="Content"
                                          Focusable="False"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background"
                                    Value="{DynamicResource PrimaryGrayColor}"/>
                                <Setter Property="Foreground"
                                    Value="{DynamicResource SecondaryBlueColor}"/>

                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>


            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5" />
                </Style>
            </Style.Resources>
        </Style>
        <!-- End: Button Style -->

        <!-- Start: Button Style -->
        <Style TargetType="Button" x:Key="GeneralButtonStyle2">
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TertiaryTextColor}"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid SnapsToDevicePixels="True">
                            <Border CornerRadius="5"
                        Background="{TemplateBinding Background}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        Margin="{TemplateBinding Margin}"/>
                            <ContentPresenter HorizontalAlignment="Center"
                                  VerticalAlignment="Center"
                                  SnapsToDevicePixels="True"
                                  RecognizesAccessKey="True"
                                  Margin="{TemplateBinding Margin}"
                                  ContentSource="Content"
                                  Focusable="False"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background"
                            Value="{DynamicResource PrimaryGrayColor}"/>
                                <Setter Property="Foreground"
                            Value="{DynamicResource SecondaryBlueColor}"/>

                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>


            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5" />
                </Style>
            </Style.Resources>
        </Style>
        <!-- End: Button Style -->
    </UserControl.Resources>

    <Grid Background="#C2222222">
        <Border x:Name="bdrProtocol" Background="{DynamicResource PrimaryTextColor}" BorderBrush="Black" BorderThickness="0" CornerRadius="5" 
                HorizontalAlignment="Center" VerticalAlignment="Center" Height="600" Width="840">
            <Border.RenderTransform>
                <TranslateTransform/>
            </Border.RenderTransform>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="520" />
                    <RowDefinition Height="80" />
                </Grid.RowDefinitions>
                <TabControl x:Name="tabCtrlConfig" FontSize="16" Style="{DynamicResource TabControlStyle}" Width="800" Height="Auto" VerticalAlignment="Top" Margin="20">
                    <TabItem x:Name="tabGeneral" Header="General Settings" Style="{DynamicResource TabItemStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <GroupBox Margin="12,8" Header="Preliminary Settings" Foreground="{DynamicResource TertiaryTextColor}" FontSize="18.667" BorderBrush="#FF0046D5">
                                <UniformGrid Rows="3">
                                    <UserControls:TextSlider x:Name="sliderFlexMax" Text="{Binding FlexionName, ElementName=uiProtocol, StringFormat=Max {0} [deg]:}" Maximum="90" TickFrequency="5"/>
                                    <UserControls:TextSlider x:Name="sliderExtMax" Text="{Binding ExtensionName, ElementName=uiProtocol, StringFormat=Max {0} [deg]:}" Maximum="90" TickFrequency="5"/>
                                    <UserControls:TextSlider x:Name="sliderExRange" Text="Extra Range [deg]:" />
                                </UniformGrid>
                            </GroupBox>
                            <GroupBox Grid.Row="1" Margin="12,8" Header="Other Settings" Foreground="{DynamicResource TertiaryTextColor}" FontSize="18.667" BorderBrush="#FF0046D5">
                                <UniformGrid Columns="2">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Sampling Rate [Hz]:" FontSize="16" Margin="12,0" VerticalAlignment="Center" />
                                        <ComboBox HorizontalAlignment="Left" Margin="20,0,0,0" Name="cboSampRate" Width="100" FontSize="16" Height="28" SelectedIndex="1">
                                            <ComboBoxItem>20</ComboBoxItem>
                                            <ComboBoxItem>50</ComboBoxItem>
                                            <ComboBoxItem>100</ComboBoxItem>
                                        </ComboBox>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Save Data File:" FontSize="16" Margin="12,0" VerticalAlignment="Center" />
                                        <ToggleButton x:Name="tgBtnSave" Width="120" Height="40" Margin="5" Click="tgBtnSave_Click" BorderThickness="0">
                                            <Ellipse x:Name="ellipseSaveing" Width="24" Height="24" Fill="Red" />
                                        </ToggleButton>
                                    </StackPanel>
                                </UniformGrid>
                            </GroupBox>
                        </Grid>
                    </TabItem>

                    <TabItem Header="Passive Stretching" x:Name="tabStretch" Style="{DynamicResource TabItemStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <GroupBox Margin="12,8" Header="General" FontSize="18.667" BorderBrush="#FF0046D5" Foreground="{DynamicResource TertiaryTextColor}">
                                <UniformGrid Columns="2">
                                    <UserControls:TextSlider x:Name="sliderStretchDuration" Text="Duration [min]:" Minimum="1" Maximum="100"/>
                                    <UserControls:TextSlider x:Name="sliderStretchHoldTime"  Text="Holding Time [sec]:" Maximum="100" />
                                </UniformGrid>
                            </GroupBox>
                            <GroupBox Grid.Row="1" Margin="12,8" Header="Velocity [deg/sec]" FontSize="18.667" BorderBrush="#FF0046D5" Foreground="{DynamicResource TertiaryTextColor}">
                                <UniformGrid Columns="2">
                                    <UserControls:TextSlider x:Name="sliderStretchFlexV" Minimum="5" Maximum="60" TickFrequency="5" 
                                                             Text="{Binding Path=FlexionName, ElementName=uiProtocol, StringFormat={}{0}:}" />
                                    <UserControls:TextSlider x:Name="sliderStretchExtV" Minimum="5" Maximum="30" TickFrequency="5" 
                                                             Text="{Binding Path=ExtensionName, ElementName=uiProtocol, StringFormat={}{0}:}" />
                                </UniformGrid>
                            </GroupBox>
                            <GroupBox Grid.Row="2" Margin="12,8" Header="Torque Limit [Nm]" FontSize="18.667" BorderBrush="#FF0046D5" Foreground="{DynamicResource TertiaryTextColor}">
                                <UniformGrid Columns="2">
                                    <UserControls:TextSlider x:Name="sliderStretchFlexTq" Minimum="1" Maximum="{Binding FlexionTorqueMax, ElementName=uiProtocol}"  
                                                             Text="{Binding Path=FlexionName, ElementName=uiProtocol, StringFormat={}{0}:}" />
                                    <UserControls:TextSlider x:Name="sliderStretchExtTq"  Minimum="1" Maximum="{Binding ExtensionTorqueMax, ElementName=uiProtocol}"  
                                                             Text="{Binding Path=ExtensionName, ElementName=uiProtocol, StringFormat={}{0}:}" />
                                </UniformGrid>
                            </GroupBox>
                        </Grid>
                    </TabItem>

                    <TabItem Header="Active Movement" x:Name="tabGames" Style="{DynamicResource TabItemStyle}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <GroupBox x:Name="groupAssist" Margin="12,8" Header="Assistive Settings" FontSize="18.667" BorderBrush="#FF0046D5" Visibility="Visible" Foreground="{DynamicResource TertiaryTextColor}">
                                <UniformGrid Rows="2" Margin="0,10">
                                    <UserControls:TextSlider x:Name="sliderAssistLevel" Text="Assistive Level:" Visibility= "Collapsed" />
                                    <UserControls:TextSlider x:Name="sliderAssistV" Text="Assistive Velocity:" Maximum="60" TickFrequency="5"/>
                                    <UserControls:TextSlider x:Name="sliderAssistDelay" Text="Delay Time [sec]:" />
                                </UniformGrid>
                            </GroupBox>

                            <GroupBox x:Name="groupResist" Grid.Row="1" Margin="12,8" Header="Resistive Settings" FontSize="18.667" BorderBrush="#FF0046D5" Foreground="{DynamicResource TertiaryTextColor}">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <UniformGrid Columns="2">
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                            <TextBlock Text=" Control Direction:" FontSize="16" Margin="12,0" VerticalAlignment="Center" />
                                            <ComboBox HorizontalAlignment="Left" Margin="20,0,0,0" Name="cboCtrlDirection" Width="100" FontSize="16" Height="28">
                                                <ComboBoxItem>X</ComboBoxItem>
                                                <ComboBoxItem>Y</ComboBoxItem>
                                            </ComboBox>
                                        </StackPanel>
                                        <UserControls:UserCheckBox x:Name="chkNoLoading" Height="36" Text=" No Loading..." FontSize="16" IsChecked="True" />
                                    </UniformGrid>
                                    <UserControls:TextSlider x:Name="sliderRessistScale" Grid.Row="1" Text="Scaling Factor:" Maximum="20" />
                                    <UniformGrid Grid.Row="2" Columns="2" IsEnabled="{Binding IsChecked, ElementName=chkNoLoading, Mode=Default, Converter={StaticResource boolReverseConverter}}">
                                        <UserControls:TextSlider x:Name="sliderResistFlex" Text="{Binding Path=FlexionName, ElementName=uiProtocol, StringFormat={}{0} Resistance:}" />
                                        <UserControls:TextSlider x:Name="sliderResistExt" Text="{Binding Path=ExtensionName, ElementName=uiProtocol, StringFormat={}{0} Resistance:}" />
                                    </UniformGrid>
                                </Grid>
                            </GroupBox>
                        </Grid>
                    </TabItem>
                </TabControl>

                <Grid Grid.Row="1" VerticalAlignment="Bottom" Margin="0,0,0,30" >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Button x:Name="btnSaveDefault" Style="{DynamicResource GeneralButtonStyle}" Content="Save" Width="150" Click="btnSaveDefault_Click"/>
                    <Button x:Name="btnCancelProtocol" Grid.Column="2" Content="Cancel" Style="{DynamicResource GeneralButtonStyle2}" Width="150" Click="btnCancelProtocol_Click"/>
                    <Button x:Name="btnAcceptProtocol" Grid.Column="3" Style="{DynamicResource GeneralButtonStyle}" Width="150" Content="Accept" Click="btnAcceptProtocol_Click"/>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
